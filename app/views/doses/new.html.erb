<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.error_notification %>

  <%= f.input :description %>
  <%= f.association :ingredient %> <%# crée une collection pour récupérer les ingrédients%>

  <%= f.button :submit, class: "btn btn-primary"%>
<% end %>

